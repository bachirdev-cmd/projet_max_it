services:
  - type: web
    name: maxit-app
    env: php
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      php setup-production.php
      echo "Build completed - railway fix applied"
    startCommand: php -S 0.0.0.0:$PORT -t public/
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_URL
        value: https://projet-max-it.onrender.com
      # Variables Railway Database (seule DATABASE_URL nécessaire)
      - key: DATABASE_URL
        value: postgresql://postgres:NvkogRfRpUphAVmRnzpiKKUSKnHTwQMw@yamabiko.proxy.rlwy.net:16680/railway

# Base de données Railway (externe) - pas besoin de section databases
